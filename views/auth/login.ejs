<%- include('../includes/head.ejs') %>
</head>

<body>
   <%- include('../includes/navigation.ejs') %>
    <div class="background">
    <main>
        <h1><i>Vui lòng đăng nhập trước khi bắt đầu...</i></h1>
        <% if(errorMessage){ %>
            <div class="user-message user-message--error">
                <%= errorMessage %>
            </div>
            <% } %>
            <form class="login-form" action="/login" method="POST">
                <div class="form-control">
                    <label for="email">Email</label>
                    <input
                        type="email" 
                        name="email" 
                        id="email" 
                        value="<%= oldInput.email %>"
                        >
                </div>
                <div class="form-control">
                    <label for="password">Mật khẩu</label>
                    <input 
                       
                        type="password" 
                        name="password" 
                        id="password" 
                        value="<%= oldInput.password %>"
                        >
                </div>
                <button class="btn" type="submit">Đăng nhập</button> 
            </form>

            <div class="centered">
                <h2><i>Để test ứng dụng vui lòng nhập</i></h2>
                <p>Email phân quyền cho quản lý : quanly@gmail.com</p>
                <p>Mật khẩu : 123456</p>
                <p>Email phân quyền cho nhân viên : nhanvien@gmail.com</p>
                <p>Mật khẩu : 123456</p>
            </div>
    </main>
</div>
<%- include('../includes/end.ejs') %>